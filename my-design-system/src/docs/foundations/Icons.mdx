import { useState } from 'react';
import { Meta } from '@storybook/addon-docs/blocks';
import { Icon } from '../../components/Icon';

<Meta title="Foundations/Icons" />

export const scrollToSection = (event, id) => {
  event.preventDefault();
  document.getElementById(id)?.scrollIntoView({ behavior: 'smooth', block: 'start' });
};

{/*
  Curated sample of the icon set — Material Symbols has thousands of glyphs,
  this is a wide (but still partial) slice grouped by common UI purpose, not
  the full catalog. Any valid name from fonts.google.com/icons works with
  the `Icon` component even if it isn't listed here.
*/}
export const iconGroups = [
  {
    label: 'Navigation',
    icons: ['home', 'menu', 'search', 'close', 'arrow_back', 'arrow_forward', 'arrow_upward', 'arrow_downward', 'expand_more', 'expand_less', 'chevron_left', 'chevron_right', 'more_vert', 'more_horiz'],
  },
  {
    label: 'Actions',
    icons: ['add', 'add_circle', 'remove', 'remove_circle', 'edit', 'delete', 'delete_forever', 'save', 'download', 'upload', 'share', 'content_copy', 'content_paste', 'refresh', 'sync', 'filter_list', 'sort', 'settings', 'tune', 'print'],
  },
  {
    label: 'Visibility & Security',
    icons: ['visibility', 'visibility_off', 'lock', 'lock_open', 'block', 'verified_user', 'admin_panel_settings', 'key', 'shield', 'security'],
  },
  {
    label: 'Status & Feedback',
    icons: ['check_circle', 'cancel', 'error', 'warning', 'info', 'help', 'help_outline', 'notifications', 'notifications_off', 'notifications_active', 'star', 'star_border', 'favorite', 'favorite_border', 'thumb_up', 'thumb_down', 'report', 'new_releases'],
  },
  {
    label: 'Communication',
    icons: ['mail', 'mail_outline', 'chat', 'chat_bubble', 'call', 'call_end', 'send', 'forum', 'campaign', 'sms', 'alternate_email', 'video_call'],
  },
  {
    label: 'Media',
    icons: ['play_arrow', 'play_circle', 'pause', 'stop', 'skip_next', 'skip_previous', 'fast_forward', 'fast_rewind', 'volume_up', 'volume_off', 'image', 'photo_camera', 'videocam', 'mic', 'mic_off', 'music_note'],
  },
  {
    label: 'Files & Data',
    icons: ['folder', 'folder_open', 'description', 'article', 'attach_file', 'cloud_upload', 'cloud_download', 'insert_drive_file', 'table_chart', 'bar_chart', 'pie_chart', 'analytics', 'backup', 'save_alt', 'folder_zip', 'insert_chart'],
  },
  {
    label: 'User & Account',
    icons: ['person', 'person_add', 'group', 'groups', 'account_circle', 'badge', 'logout', 'login', 'manage_accounts', 'switch_account', 'face', 'contacts'],
  },
  {
    label: 'Commerce',
    icons: ['shopping_cart', 'shopping_bag', 'add_shopping_cart', 'credit_card', 'payments', 'local_shipping', 'storefront', 'receipt_long', 'sell', 'discount', 'loyalty', 'wallet'],
  },
  {
    label: 'Layout & Misc',
    icons: ['dashboard', 'calendar_today', 'calendar_month', 'event', 'schedule', 'alarm', 'timer', 'location_on', 'my_location', 'link', 'apps', 'grid_view', 'list', 'view_module', 'dark_mode', 'light_mode'],
  },
  {
    label: 'Devices & Tech',
    icons: ['computer', 'laptop', 'smartphone', 'tablet_mac', 'tv', 'watch', 'keyboard', 'mouse', 'wifi', 'bluetooth', 'battery_full', 'memory', 'storage', 'router'],
  },
  {
    label: 'Travel & Places',
    icons: ['flight', 'directions_car', 'directions_bus', 'directions_walk', 'directions_bike', 'train', 'hotel', 'restaurant', 'local_cafe', 'beach_access', 'hiking', 'map', 'place', 'near_me', 'luggage', 'sailing'],
  },
  {
    label: 'Health & Science',
    icons: ['local_hospital', 'medical_services', 'healing', 'vaccines', 'science', 'biotech', 'psychology', 'fitness_center', 'spa', 'monitor_heart'],
  },
  {
    label: 'Weather & Nature',
    icons: ['wb_sunny', 'nights_stay', 'umbrella', 'ac_unit', 'thermostat', 'water_drop', 'eco', 'park', 'pets', 'forest'],
  },
  {
    label: 'Language & Region',
    icons: ['language', 'translate', 'public', 'flag', 'home_work', 'business'],
  },
];

{/* Searchable grid — filters every group by substring match, click-to-copy the `<Icon name="..." />` snippet. */}
export const IconLibrary = () => {
  const [query, setQuery] = useState('');
  const [copied, setCopied] = useState(null);

  const handleCopy = (name) => {
    navigator.clipboard?.writeText(`<Icon name="${name}" />`);
    setCopied(name);
    setTimeout(() => setCopied((current) => (current === name ? null : current)), 1200);
  };

  const normalizedQuery = query.trim().toLowerCase();
  const filteredGroups = iconGroups
    .map((group) => ({
      ...group,
      icons: group.icons.filter((name) => name.includes(normalizedQuery)),
    }))
    .filter((group) => group.icons.length > 0);

  return (
    <div>
      <input
        type="text"
        value={query}
        onChange={(e) => setQuery(e.target.value)}
        placeholder="Search icons by name…"
        className="mb-8 w-full rounded-md border border-primary bg-primary px-4 py-2.5 text-sm text-primary placeholder:text-quaternary focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-brand-500"
      />

      {filteredGroups.length === 0 ? (
        <p className="text-sm text-tertiary">No icon in this sample matches "{query}" — any valid Material Symbols name still works with the component even if it isn't listed here.</p>
      ) : (
        filteredGroups.map((group) => (
          <div key={group.label} className="foundation-group-block">
            <span className="foundation-group-label block text-sm font-semibold text-primary">{group.label}</span>
            <div className="grid grid-cols-3 gap-x-2 gap-y-5 sm:grid-cols-4 md:grid-cols-6">
              {group.icons.map((name) => (
                <button
                  key={name}
                  type="button"
                  onClick={() => handleCopy(name)}
                  title={`Click to copy <Icon name="${name}" />`}
                  className="flex flex-col items-center gap-2 rounded-lg border border-secondary bg-secondary px-3 py-4 text-center transition-colors hover:border-brand hover:bg-brand-primary"
                >
                  <Icon name={name} size="lg" className="text-primary" />
                  <span className="font-mono text-tertiary" style={{ fontSize: '11px', wordBreak: 'break-all' }}>
                    {copied === name ? 'Copied!' : name}
                  </span>
                </button>
              ))}
            </div>
          </div>
        ))
      )}
    </div>
  );
};

# Icons

The Design System's single, official icon set — [Google Material Symbols](https://fonts.google.com/icons), a variable font loaded once globally (`.storybook/preview-head.html` / `index.html`). No SVG library, no emoji — every icon in the product goes through the [`Icon`](?path=/docs/components-icon--docs) component and a name from this catalog.

<div className="border-b border-secondary mb-8" />

<nav className="flex flex-wrap gap-lg bg-secondary border border-secondary rounded-lg px-6 py-4 mb-8 text-sm">
  <a href="#usage" onClick={(e) => scrollToSection(e, 'usage')} className="text-brand-secondary font-semibold">Usage</a>
  <a href="#library" onClick={(e) => scrollToSection(e, 'library')} className="text-brand-secondary font-semibold">Icon Library</a>
</nav>

<h2 id="usage" className="text-2xl font-semibold">Usage</h2>

```jsx
import { Icon } from 'my-design-system';

<Icon name="home" />
<Icon name="favorite" size="lg" filled />
<Icon name="settings" label="Settings" />
```

This is a representative sample, not the full catalog — Material Symbols has thousands of glyphs. Any valid name from [fonts.google.com/icons](https://fonts.google.com/icons) works with `<Icon name="..." />`, listed here or not. For the full API (`size`, `filled`, `weight`, `color`, `label`) and accessibility guidance, see the [`Icon` component page](?path=/docs/components-icon--docs).

<h2 id="library" className="text-2xl font-semibold">Icon Library</h2>

<IconLibrary />
